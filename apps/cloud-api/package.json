{
  "name": "@hrkit/cloud-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers ingest API for @hrkit — accepts HR samples and fans them out to Durable Objects per athlete.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.lint.json"
  },
  "dependencies": {
    "@hrkit/core": "workspace:*",
    "@hrkit/edge": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.84.0"
  }
}
